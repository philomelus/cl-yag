(in-package #:cl-yag)

(docs:define-docs (defun print-raw-object
                    "Print an object in #<...> notation."))

(docs:define-docs (defun rect-collide
                    "Returns t if the rectangle coordinates intersect."))
